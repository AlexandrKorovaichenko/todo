import {userReducer} from "./user-reducer"


test("use reducer should increment only childrenCount", () => {

    // const startState = {age: 20, childrenCount: 2, name: "Dima"}
    // const endState = userReducer(startState, { type: "INCREMENT-CHILDREN" })

    // expect(endState.childrenCount).toBe(3);
    // expect(endState.age).toBe(20);

})


test("use reducer should name of user", () => {

    // const oldState = {name: "Dima", 
    //                     childrenCount: 2, 
    //                     age: 20
    //                 }

    // const newSate = userReducer(oldState, {type: "CHANGE-NAME", newName: "Victor"});
    // expect(oldState.name).toBe("Dima");
    // expect(newSate.name).toBe("Victor");

})